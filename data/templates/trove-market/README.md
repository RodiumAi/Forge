# trove-market

Mobile app-shell template (React). Shopping: onboarding + navbar + Home feed/product grid + bottom tabs (Home, Search, Cart, Account).

- `npm install` / `npm run dev`

See `DESIGN.md`.
