# calm-space

Mobile app-shell template (React). Meditation & sleep: onboarding + navbar + Home breathe/sessions + bottom tabs (Home, Explore, Sleep, Profile).

- `npm install` / `npm run dev`

See `DESIGN.md`.
