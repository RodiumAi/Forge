# tempo-run

Mobile app-shell template (React). Running tracker: onboarding + navbar + Home activity rings / start run + bottom tabs (Home, Runs, Stats, Profile).

- `npm install` / `npm run dev`

See `DESIGN.md`.
