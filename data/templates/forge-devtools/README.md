# forge-devtools

Dark developer-tool landing template (React + Vite, single-file App).

- `npm install`
- `npm run dev` — http://localhost:5173
- `npm run build`

Everything lives in `src/App.tsx` and `src/index.css`. See `DESIGN.md` for the design charter.
