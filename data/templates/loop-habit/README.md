# loop-habit

Mobile app-shell template (React). Habit tracker: onboarding + navbar + Today progress ring / streak checklist + bottom tabs (Today, Habits, Stats, Profile).

- `npm install` / `npm run dev`

See `DESIGN.md`.
