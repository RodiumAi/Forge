# Lumen Atelier

Editorial architecture studio template. React 18 + Vite + TypeScript, single-file page in `src/App.tsx`, standalone stylesheet in `src/index.css`.

- `npm install`
- `npm run dev` — http://localhost:5173
- `npm run build`

See `DESIGN.md` for the design charter (colors, tone, approved imagery).
