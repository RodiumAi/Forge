# ava-assistant

Mobile app-shell template (React). AI assistant: onboarding + navbar + Chat thread + bottom tabs (Chat, Prompts, History, Account).

- `npm install` / `npm run dev`

See `DESIGN.md`.
