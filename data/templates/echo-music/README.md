# echo-music

Mobile app-shell template (React). Echo: onboarding + navbar + Home + bottom tabs (Home, Search, Library, Now) — music streaming player with a persistent mini-player.

- `npm install` / `npm run dev`

See `DESIGN.md`.
