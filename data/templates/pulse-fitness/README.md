# Pulse Club

Energetic fitness club template — diagonal clip-path sections, electric lime on black, animated progress bars. React 18 + Vite + TypeScript, single-file page in `src/App.tsx`.

- `npm install`
- `npm run dev` — http://localhost:5173
- `npm run build`

See `DESIGN.md` for the design charter.
