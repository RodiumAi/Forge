# fern-plants

Mobile app-shell template (React). Plant care & watering reminders: onboarding + navbar + Home water-today / my-plants + bottom tabs (Home, Explore, Add, Profile).

- `npm install` / `npm run dev`

See `DESIGN.md`.
