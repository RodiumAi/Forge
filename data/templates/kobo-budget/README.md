# kobo-budget

Mobile app-shell template (React). Kobo budgeting app: onboarding + navbar + Home budget-left ring + category progress + bottom tabs (Home, Add, Stats, Account).

- `npm install` / `npm run dev`

See `DESIGN.md`.
