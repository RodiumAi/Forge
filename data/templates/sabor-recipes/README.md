# sabor-recipes

Mobile app-shell template (React). Recipes & cooking: onboarding + navbar + Home featured recipe/categories + bottom tabs (Home, Search, Saved, Profile).

- `npm install` / `npm run dev`

See `DESIGN.md`.
