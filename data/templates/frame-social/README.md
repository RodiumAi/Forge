# frame-social

Mobile app-shell template (React). Photo social network: onboarding + navbar + Feed with stories + bottom tabs (Feed, Explore, Create, Profile).

- `npm install` / `npm run dev`

See `DESIGN.md`.
