# rida-ride

Mobile app-shell template (React). Ride-hailing: onboarding + navbar + Home map/ride-options + bottom tabs (Home, Trips, Activity, Account).

- `npm install` / `npm run dev`

See `DESIGN.md`.
